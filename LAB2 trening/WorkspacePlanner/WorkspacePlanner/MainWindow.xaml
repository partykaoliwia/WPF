<Window x:Class="WorkspacePlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Interactive Planner" Height="700" Width="1000"
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0"
                Background="Pink">
            <Canvas x:Name="WorkspaceCanvas"/>
        </Border>
        <StackPanel Grid.Column="1">
            <Button Content="Add Rectangle"
                    Margin="70,10,70,0"
                    Click="Button_AddRectangle"/>
            <Button Content="Add Elipse"
                    Margin="70,10,70,10"
                    Click="Button_AddEllipse"/>
            <TextBlock Text="Loading shape..." HorizontalAlignment="Center"/>
            <ProgressBar x:Name="ShapeProgressBar"
                         Height="20" Margin="5"
                         Minimum="0" Maximum="100" Value="0"/>
            <DataGrid x:Name="ShapesDataGrid"
                      ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"
                                        Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTextColumn Header="Type"
                                        Binding="{Binding ShapeType, UpdateSourceTrigger=PropertyChanged}"
                                        IsReadOnly="True"/>
                    <DataGridTextColumn Header="X"
                                        Binding="{Binding X, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTextColumn Header="Y"
                                        Binding="{Binding Y, UpdateSourceTrigger=PropertyChanged}"/>
                    
                </DataGrid.Columns>
                
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
