<Window x:Class="Libraria.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Libraria"
        mc:Ignorable="d"
        Title="Libraria" 
        Height="650" MinHeight="500" 
        Width="1000" MinWidth="920"
        Icon="h:\Windows7\Desktop\PŚG\wpf3_partyka_oliwia\Libraria\Libraria\Resources\book.ico"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>
        <!-- MENU -->
        <Menu Grid.Row="0">
            <MenuItem Header="Repository"/>
            <MenuItem Header="Book">
                <MenuItem Header="Add..." Click="MenuItem_AddBook" />
                <MenuItem Header="Edit..."/>
                <MenuItem Header="Delete..." Click="MenuItem_DeleteBook" InputGestureText="Del"/>
            </MenuItem>
        </Menu>
        <!-- GÓRNY PASEK -->
        <Grid Grid.Row="1" Background="{StaticResource PodstawowyBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0">
                <Image Source="/Resources/book.png"
                       Margin="15,0,0,0"/>
            </Border>
            <Grid Grid.Column="1">
                <StackPanel>
                    <TextBlock FontFamily="Segoe Script"
                               FontSize="20"
                               FontWeight="Bold"
                               Text="Libraria"
                               Foreground="White"
                               Margin="10,10,0,0"/>
                    <TextBlock Text="Personal Book Manager"
                               FontSize="10"
                               Foreground="Gray"
                               Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
        <!-- WIDOK KSIĄŻEK -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook}" BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                <WrapPanel Margin="10"/>
                    
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="130" Margin="10">
                        <Border Width="120" Height="160" CornerRadius="3" Background="{StaticResource AkcentBrush}" ClipToBounds="True">
                            <Image Source="{Binding CoverImagePath}" Stretch="UniformToFill"/>
                        </Border>
                        <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="5,5,5,0"/>
                        <TextBlock Text="{Binding Author}" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <!-- DOLNY PASEK -->
    </Grid>
</Window>
